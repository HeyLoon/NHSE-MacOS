<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:NHSE.macOS.ViewModels.Player"
             x:Class="NHSE.macOS.Views.Player.RecipeEditorView"
             x:DataType="vm:RecipeEditorViewModel"
             Width="400" Height="600">

    <Grid RowDefinitions="Auto,*,Auto" Margin="10">
        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Margin="0,0,0,10">
            <Button Content="Set All Known" Command="{Binding SetAllKnownCommand}"/>
            <Button Content="Set All Unknown" Command="{Binding SetAllUnknownCommand}"/>
            <Button Content="Clear New Flags" Command="{Binding ClearNewFlagsCommand}"/>
        </StackPanel>

        <!-- Recipe List -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Recipes}" AutoGenerateColumns="False" IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding RecipeId}" Width="60" IsReadOnly="True"/>
                <DataGridTextColumn Header="Recipe" Binding="{Binding RecipeName}" Width="*" IsReadOnly="True"/>
                <DataGridCheckBoxColumn Header="Known" Binding="{Binding IsKnown}" Width="70"/>
                <DataGridCheckBoxColumn Header="New" Binding="{Binding IsNew}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="10" Margin="0,10,0,0">
            <Button Content="Cancel" IsCancel="True"/>
            <Button Content="Save" Command="{Binding SaveCommand}" IsDefault="True"/>
        </StackPanel>
    </Grid>
</UserControl>
